import 'package:flutter/material.dart';

class HomeDispatchView extends StatelessWidget {
  const HomeDispatchView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
